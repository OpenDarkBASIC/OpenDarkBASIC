#pragma once

#define STRINGIFY_(x) #x
#define STRINGIFY(x) STRINGIFY_(x)

#define ODBI_VERSION_MAJOR ${PROJECT_VERSION_MAJOR}
#define ODBI_VERSION_MINOR ${PROJECT_VERSION_MINOR}
#define ODBI_VERSION_PATCH ${PROJECT_VERSION_PATCH}
#define ODBI_VERSION \
    ((ODBI_VERSION_MAJOR << 16) | (ODBI_VERSION_MINOR << 8) | ODBI_VERSION_PATCH)
#define ODBI_VERSION_STR \
    STRINGIFY(ODBI_VERSION_MAJOR) "." STRINGIFY(ODBI_VERSION_MINOR) "." STRINGIFY(ODBI_VERSION_PATCH)

