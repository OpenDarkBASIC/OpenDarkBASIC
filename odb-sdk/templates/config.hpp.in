#pragma once

#define STRINGIFY_(x) #x
#define STRINGIFY(x) STRINGIFY_(x)

#define ODBRUNTIME_VERSION_MAJOR ${PROJECT_VERSION_MAJOR}
#define ODBRUNTIME_VERSION_MINOR ${PROJECT_VERSION_MINOR}
#define ODBRUNTIME_VERSION_PATCH ${PROJECT_VERSION_PATCH}
#define ODBRUNTIME_VERSION \
    ((ODBRUNTIME_VERSION_MAJOR << 16) | (ODBRUNTIME_VERSION_MINOR << 8) | ODBRUNTIME_VERSION_PATCH)
#define ODBRUNTIME_VERSION_STR \
    STRINGIFY(ODBRUNTIME_VERSION_MAJOR) "." STRINGIFY(ODBRUNTIME_VERSION_MINOR) "." STRINGIFY(ODBRUNTIME_VERSION_PATCH)

#define ODBRUNTIME_PLATFORM_${ODB_PLATFORM}
#define ODBRUNTIME_${ODBRUNTIME_LIB_TYPE}

#if defined(ODBRUNTIME_SHARED)
#   if defined(ODBRUNTIME_BUILDING)
#       define ODBRUNTIME_PUBLIC_API ${ODBRUNTIME_API_EXPORT}
#   else
#       define ODBRUNTIME_PUBLIC_API ${ODBRUNTIME_API_IMPORT}
#   endif
#   define ODBRUNTIME_PRIVATE_API ${ODBRUNTIME_API_LOCAL}
#else
#   define ODBRUNTIME_PUBLIC_API
#   define ODBRUNTIME_PRIVATE_API
#endif
