#pragma once

#define STRINGIFY_(x) #x
#define STRINGIFY(x) STRINGIFY_(x)

#define ODBC_VERSION_MAJOR ${PROJECT_VERSION_MAJOR}
#define ODBC_VERSION_MINOR ${PROJECT_VERSION_MINOR}
#define ODBC_VERSION_PATCH ${PROJECT_VERSION_PATCH}
#define ODBC_VERSION \
    ((ODBC_VERSION_MAJOR << 16) | (ODBC_VERSION_MINOR << 8) | ODBC_VERSION_PATCH)
#define ODBC_VERSION_STR \
    STRINGIFY(ODBC_VERSION_MAJOR) "." STRINGIFY(ODBC_VERSION_MINOR) "." STRINGIFY(ODBC_VERSION_PATCH)
#define ODBC_${ODBC_LIB_TYPE}
#cmakedefine ODBC_DOT_EXPORT
#cmakedefine ODBC_VERBOSE_BISON
#cmakedefine ODBC_VERBOSE_FLEX

#define ODBC_WIN32 0
#define ODBC_MACOS 1
#define ODBC_LINUX 2
#if defined(__WIN32__) || defined(_WIN32)
#define ODBC_PLATFORM ODBC_WIN32
#elif defined(__APPLE_CC__)
#define ODBC_PLATFORM ODBC_MACOS
#elif defined(__linux__) && !defined(__ANDROID__)
#define ODBC_PLATFORM ODBC_LINUX
#endif

#if defined(ODBC_SHARED)
#   if defined(ODBC_BUILDING)
#       define ODBC_PUBLIC_API ${ODBC_API_EXPORT}
#   else
#       define ODBC_PUBLIC_API ${ODBC_API_IMPORT}
#   endif
#   define ODBC_PRIVATE_API ${ODBC_API_LOCAL}
#else
#   define ODBC_PUBLIC_API
#   define ODBC_PRIVATE_API
#endif

